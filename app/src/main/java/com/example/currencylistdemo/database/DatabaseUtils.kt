package com.example.currencylistdemo.database

const val DATABASE_NAME = "CurrencyData.db"

class DatabaseUtils {
    companion object {
        const val KEY_ID = "id"
        const val KEY_NAME = "name"
        const val KEY_SYMBOL = "symbol"
    }
}